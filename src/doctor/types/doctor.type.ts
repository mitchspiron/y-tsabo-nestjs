export type Doctor = {
  idDoctor: number;
  lastnameDoctor: string;
  firstnameDoctor: string;
  emailDoctor: string;
  phoneDoctor: string;
  addressDoctor: string;
  speciality: number;
};

export type DoctorPassword = {
  passwordDoctor: string;
};
